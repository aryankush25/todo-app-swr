import { createMuiTheme } from '@material-ui/core/styles';

// Checkout https://material-ui.com/customization/default-theme/
// To customize theme

const mainTheme = createMuiTheme({
  palette: {
    type: 'dark'
  }
});

export default mainTheme;
